Starting SQL:

1. mysql -uroot < sql-queries/creates.sql
2. mysql -uroot < sql-queries/insterts.sql

Starting flask
3. ./run

Looking at the database:

1. mysql -uroot
2. use 471_project;       (Or any other database made)
3. list tables
